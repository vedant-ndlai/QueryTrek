version: '3.8'

services:
  postgres:
    image: postgres:latest
    container_name: iris-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

    networks:
      - vmigrateagent-network

volumes:
  postgres-data:

networks:
  vmigrateagent-network:
    external: true
